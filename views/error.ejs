<!DOCTYPE html>
<html lang="en">
<%- include('partials/head', { title: title || 'Error' }) %>
<body>
  <%- include('partials/navbar') %>
  <main class="container" style="padding: var(--space-2xl) var(--space-lg); text-align:center;">
    <h1><%= title || 'Something went wrong' %></h1>
    <p class="muted"><%= message || 'Please try again later.' %></p>
    <a href="/" class="btn btn-primary">Back to home</a>
  </main>
  <%- include('partials/footer') %>
</body>
</html>
